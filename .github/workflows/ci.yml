name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  android:
    name: Android Build, Test & Quality
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: android
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: ${{ runner.os }}-gradle-

      - name: Build
        run: ./gradlew build

      - name: Test
        run: ./gradlew test

      # Code quality checks (merged from separate job — saves a runner)
      - name: Check for emojis in production code
        working-directory: .
        run: |
          if grep -rPn '[\x{1F600}-\x{1F64F}\x{1F300}-\x{1F5FF}\x{1F680}-\x{1F6FF}\x{1F1E0}-\x{1F1FF}\x{2702}-\x{27B0}\x{FE00}-\x{FE0F}\x{1F900}-\x{1F9FF}]' android/src/ ios/Classes/ 2>/dev/null; then
            echo "::error::Emojis found in production code"
            exit 1
          fi

      - name: Check for internal files
        working-directory: .
        run: |
          for file in CLAUDE.md .cursorrules; do
            if [ -f "$file" ] && git ls-files --error-unmatch "$file" 2>/dev/null; then
              echo "::error::Internal file $file is tracked in git"
              exit 1
            fi
          done
          for dir in tasks/ prompts/ .claude/; do
            if git ls-files | grep -q "^${dir}"; then
              echo "::error::${dir} directory is tracked in git"
              exit 1
            fi
          done

      - name: Check for secrets
        working-directory: .
        run: |
          if grep -rPn '(sk_live_|sk_test_|pk_live_|pk_test_|AKIA[A-Z0-9]{16}|ghp_[a-zA-Z0-9]{36}|xoxb-|xoxp-)' --include="*.kt" --include="*.swift" --include="*.java" --include="*.xml" --include="*.yaml" --include="*.yml" --include="*.json" --include="*.properties" . 2>/dev/null | grep -v '.gradle/' | grep -v 'node_modules'; then
            echo "::error::Potential secrets found in code"
            exit 1
          fi

      - name: Check for internal references
        working-directory: .
        run: |
          if grep -rn 'Teslon\|Sector7' --include="*.kt" --include="*.swift" android/src/ ios/Classes/ 2>/dev/null; then
            echo "::error::Internal references found in production code"
            exit 1
          fi

  ios:
    name: iOS Pod Lint
    runs-on: macos-latest
    # Only run when iOS-related files change (macOS runners cost 10x)
    if: |
      github.event_name == 'pull_request' ||
      contains(toJSON(github.event.commits.*.modified), 'ios/') ||
      contains(toJSON(github.event.commits.*.added), 'ios/') ||
      contains(toJSON(github.event.commits.*.modified), '.podspec')
    defaults:
      run:
        working-directory: ios
    steps:
      - uses: actions/checkout@v4

      - name: Lint podspec
        run: pod lib lint PolyfenceCore.podspec --allow-warnings
