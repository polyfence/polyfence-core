import Foundation

/// Single source of truth for the polyfence-core library version.
/// Must be updated in sync with build.gradle.kts and PolyfenceCore.podspec.
public enum PolyfenceCoreVersion {
    public static let version = "1.0.5"
}
